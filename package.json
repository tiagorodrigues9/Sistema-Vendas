{
  "name": "sistema-pdv",
  "version": "1.0.0",
  "description": "Sistema PDV completo com Node.js, React e MongoDB",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd api && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd api && npm install && cd ../frontend && npm install"
  },
  "keywords": ["pdv", "vendas", "nodejs", "react", "mongodb"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
